# 🎯 Гайд по настройке бота

Этот файл поможет быстро найти и изменить любые настройки бота.

## 📝 Текстовые сообщения и контент

### `.env` — Основной контент
Здесь хранятся тексты, которые часто меняются:

```env
# Информация о дне рождения (отображается с фото)
BIRTHDAY_INFO=🎉 *Место проведения:*\n🏡 *Гостевой комплекс SandittenSpa*...

# Информация о выезде (отображается с геопозицией)
TRIP_INFO=📅 *Дата:* 4 апреля\n⏰ *Время:* 14:30...

# Ссылка на вишлист
WISHLIST_URL=https://mywishboard.com/@vsevolodkulikov
```

### `app/messages.py` — Все сообщения бота
Все текстовые сообщения, надписи на кнопках и эмодзи:

```python
class Messages:
    WELCOME_GROUP = "Привет! Я бот для организации дня рождения..."
    PHOTO_CONTEST_STARTED = "*КОНКУРС ФОТО НАЧАЛСЯ!*..."
    # и т.д.

class ButtonLabels:
    BIRTHDAY = "День рождения"
    TRIP = "Выезд"
    # и т.д.

class Emojis:
    BIRTHDAY = "🎂"
    TRIP = "🚗"
    # и т.д.
```

## 🖼️ Медиа и геолокация

### `app/constants.py` — Технические константы

```python
# МЕДИА-РЕСУРСЫ
BIRTHDAY_PHOTO_1 = ASSETS_DIR / "photo1.jpg"  # Первое фото для /birthday
BIRTHDAY_PHOTO_2 = ASSETS_DIR / "photo2.jpg"  # Второе фото для /birthday

# ГЕОПОЗИЦИИ
# Координаты места сбора (магазин SPAR, ул. Аэропортная, 12)
TRIP_MEETING_POINT_LATITUDE = 54.731057
TRIP_MEETING_POINT_LONGITUDE = 20.554409

# ЛИМИТЫ И НАСТРОЙКИ
MAX_PHOTO_CONTEST_PARTICIPANTS = 22  # Максимум участников фото-конкурса
MIN_TOURNAMENT_PARTICIPANTS = 4      # Минимум участников турнира
```

### `app/assets/` — Медиа-файлы
```
app/assets/
├── photo1.jpg  # Первое фото для команды /birthday
└── photo2.jpg  # Второе фото для команды /birthday
```

## 🎮 Callback-кнопки

### `app/callbacks.py` — Идентификаторы inline-кнопок

```python
class MenuCallbacks:
    BIRTHDAY = "menu:birthday"
    TRIP = "menu:trip"
    # и т.д.
```

## 🔧 Конфигурация окружения

### `.env` — Токены и ID

```env
BOT_TOKEN=your_bot_token          # Токен бота от @BotFather
ADMIN_ID=your_telegram_id         # Ваш Telegram ID
GROUP_ID=group_chat_id            # ID группового чата
YANDEX_MUSIC_TOKEN=your_token     # Токен Яндекс.Музыки (опционально)
YANDEX_PLAYLIST_KIND=playlist_id  # ID плейлиста (опционально)
```

---

## 📍 Частые задачи

### Изменить фото для команды /birthday
1. Положите новые фото в `app/assets/`
2. Обновите пути в `app/constants.py`:
   ```python
   BIRTHDAY_PHOTO_1 = ASSETS_DIR / "новое_фото1.jpg"
   BIRTHDAY_PHOTO_2 = ASSETS_DIR / "новое_фото2.jpg"
   ```

### Изменить место сбора для команды /trip
Обновите координаты в `app/constants.py`:
```python
TRIP_MEETING_POINT_LATITUDE = 55.123456
TRIP_MEETING_POINT_LONGITUDE = 37.654321
```

### Изменить текст приветствия
Отредактируйте в `app/messages.py`:
```python
WELCOME_GROUP = "Ваш новый текст приветствия"
```

### Изменить лимит участников фото-конкурса
Обновите в `app/constants.py`:
```python
MAX_PHOTO_CONTEST_PARTICIPANTS = 30  # Было 22
```

---

## ⚠️ Важно

После изменения констант:
1. **Перезапустите бота:** `python run.py`
2. **Проверьте логи** на наличие ошибок
3. **Протестируйте** изменённые команды в группе

После изменения `.env` файла бот автоматически подхватит новые значения при перезапуске.
