# Birthday Party Bot

Telegram-бот для организации дня рождения с поддержкой групповых мероприятий, конкурсов и интеграцией с Яндекс.Музыкой.

## Возможности

### Для гостей (групповой чат)

| Команда | Описание |
|---------|----------|
| `/menu` | Главное меню с inline-кнопками |
| `/birthday` | Информация о дне рождения (с фото места проведения) |
| `/trip` | Информация о выезде с геопозицией места сбора |
| `/wishlist` | Ссылка на вишлист именинника |
| `/location` | Геопозиция места проведения |
| `/ask` | Отправить сообщение организатору |
| `/help` | Список доступных команд |

### Для гостей (личные сообщения боту)

- **Добавление треков** — отправьте ссылку на трек из Яндекс.Музыки, и он автоматически добавится в плейлист вечеринки
- **Участие в фотоконкурсе** — отправьте фото во время активного конкурса

### Для организатора (админ-панель)

| Функция | Описание |
|---------|----------|
| 📸 Фото-конкурс | Запуск/остановка конкурса с автоматическим голосованием (макс. 22 участника) |
| 🏆 Турнир бир-понга | Турнирная система с автоматической генерацией команд и сетки |
| 🔪 Достать ножи | Полноценная игра с регистрацией, кольцевой системой заданий, подтверждением смертей и финальным отчётом |
| 📊 Опросы | Создание опросов с одним или несколькими вариантами ответа |
| 📢 Broadcast | Отправка объявлений в группу |
| 📍 Геопозиция | Установка и обновление места проведения |
| 🎵 Добавить трек | Добавление трека в плейлист вечеринки |
| ✉️ Ответы | Ответ на сообщения гостей напрямую из личного чата |

## Технологии

- **Python 3.10+**
- **aiogram 3.4+** — асинхронный фреймворк для Telegram Bot API
- **yandex-music** — API для работы с Яндекс.Музыкой
- **python-dotenv** — управление переменными окружения

## Структура проекта

```
├── app/
│   ├── handlers/
│   │   ├── admin.py      # Обработчики команд администратора
│   │   ├── group.py      # Обработчики группового чата
│   │   ├── user.py       # Обработчики личных сообщений
│   │   ├── tournament.py # Обработчики турнира бир-понга
│   │   └── spy_game.py   # Обработчики игры Assassin
│   ├── services/
│   │   └── yandex_music.py   # Сервис интеграции с Яндекс.Музыкой
│   ├── assets/
│   │   ├── photo1.jpg    # Фото для информации о дне рождения
│   │   └── photo2.jpg    # Фото для информации о дне рождения
│   ├── bot.py            # Инициализация и запуск бота
│   ├── callbacks.py      # Callback-идентификаторы для inline-кнопок
│   ├── config.py         # Конфигурация приложения
│   ├── constants.py      # Общие константы (regex паттерны, лимиты)
│   ├── database.py       # SQLite база данных для игры Assassin
│   ├── filters.py        # Переиспользуемые фильтры
│   ├── keyboards.py      # Клавиатуры и меню
│   ├── messages.py       # Тексты сообщений и эмодзи
│   ├── middleware.py     # Middleware (rate limiting)
│   ├── states.py         # FSM-состояния для диалогов
│   ├── storage.py        # In-memory хранилища данных с TTL
│   └── tournament_utils.py # Утилиты для турнирной системы
├── .env.example          # Пример файла конфигурации
├── requirements.txt      # Зависимости Python
└── run.py                # Точка входа
```

## Установка и запуск

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd project1
```

### 2. Создание виртуального окружения

```bash
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/macOS
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка переменных окружения

Скопируйте `.env.example` в `.env` и заполните значения:

```bash
cp .env.example .env
```

```env
# Обязательные параметры
BOT_TOKEN=your_bot_token_here
ADMIN_ID=your_telegram_id
GROUP_ID=group_chat_id

# Контент для отображения
# BIRTHDAY_INFO отображается вместе с фото (app/assets/photo1.jpg и photo2.jpg)
BIRTHDAY_INFO=🎉 *Место проведения:*\n\n🏡 *Гостевой комплекс SandittenSpa*\n📍 Поселок Лунино, Гвардейский район\n\n🔗 [sandittenspa.ru](https://sandittenspa.ru)\n\n✨ Ждём вас на незабываемом празднике!
# TRIP_INFO отображается вместе с геопозицией места сбора
TRIP_INFO=🚗 *Информация о выезде*\n\n📅 *Дата:* 4 апреля\n⏰ *Время:* 14:30\n📍 *Место сбора:* Магазин SPAR\n🏠 *Адрес:* ул. Аэропортная, 12
WISHLIST_URL=https://example.com/wishlist

# Яндекс Музыка (опционально)
YANDEX_MUSIC_TOKEN=your_yandex_music_token
YANDEX_PLAYLIST_KIND=1234
```

### 5. Запуск бота

```bash
python run.py
```

## Получение токенов и ID

### BOT_TOKEN

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot` и следуйте инструкциям
3. Скопируйте полученный токен

### ADMIN_ID

1. Откройте [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте любое сообщение
3. Скопируйте ваш ID

### GROUP_ID

1. Добавьте бота [@RawDataBot](https://t.me/RawDataBot) в группу
2. Скопируйте значение `chat.id` из ответа (отрицательное число)
3. Удалите RawDataBot из группы

### YANDEX_MUSIC_TOKEN (опционально)

1. Авторизуйтесь на [music.yandex.ru](https://music.yandex.ru)
2. Откройте DevTools (F12) → Application → Cookies
3. Скопируйте значение `Session_id`

### YANDEX_PLAYLIST_KIND

1. Откройте нужный плейлист в Яндекс.Музыке
2. Скопируйте число из URL: `music.yandex.ru/users/USERNAME/playlists/PLAYLIST_KIND`

## Игра "Достать ножи"

Бот включает полнофункциональную игру "Достать ножи" для вечеринки:

### Как играть

1. **Регистрация**: Админ открывает регистрацию, гости регистрируются в личке бота
2. **Настройка**: Админ задаёт списки оружий и локаций (есть дефолтные)
3. **Старт**: Каждый игрок получает задание: цель (другой игрок), оружие и локацию
4. **Игра**: Игроки "убивают" свои цели, жертва подтверждает смерть кнопкой "Я мёртв"
5. **Цепочка**: После смерти убийца получает цель своей жертвы с новым оружием/локацией
6. **Победа**: Игра продолжается до последнего выжившего
7. **Отчёт**: В конце — полная хронология убийств и путь победителя

### Особенности

- **Безопасная зона**: "курилка" — убийства там запрещены
- **Персистентность**: SQLite база, игра переживает перезапуск
- **Тестовый режим**: Админ может прогнать игру с виртуальными игроками
- **Конфиденциальность**: Анонсы смертей не раскрывают убийцу
- **До 22 участников**: Масштабируется для больших компаний

### Админ-функции

- **Открытие регистрации** — объявление в группу с правилами и устрашающим сообщением
- **Настройка оружий и локаций** — кастомные списки или дефолтные значения
- **Запуск игры** — автоматическое распределение целей и отправка заданий
- **Тестовый режим** — симуляция игры с виртуальными игроками (пошаговые инструкции)
- **Сброс/перезапуск** — завершение текущей игры

### Особенности реализации

- **Персистентность на уровне кода** — SQLite база данных переживает перезапуски
- **Транзакционность** — все критические операции (смерть, назначение контрактов) атомарны
- **Подробные инструкции** — пошаговые подсказки в тестовом режиме
- **Устрашающие сообщения** — атмосфера игры поддерживается текстами
- **Timezone support** — время в отчётах отображается в заданном часовом поясе

## Деплой на Amvera

Проект включает конфигурацию для деплоя на [Amvera](https://amvera.ru):

```yaml
# amvera.yml
meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"

build:
  requirementsPath: requirements.txt

run:
  scriptName: run.py
```

## Архитектура

### Модульная структура обработчиков

Бот использует систему роутеров aiogram для разделения логики:

- `admin_router` — команды администратора (личный чат)
- `group_router` — команды группового чата
- `user_router` — команды обычных пользователей (личный чат)

### FSM (Finite State Machine)

Для многошаговых диалогов используются состояния:

- `AskState` — отправка сообщения организатору
- `AddTrackState` — добавление трека в плейлист
- `GeoState` — установка геопозиции
- `AdminBroadcastState` — отправка объявления
- `AdminPollState` — создание опроса (один или несколько ответов)
- `TournamentState` — управление турниром
- `AssassinState` — настройка и управление игрой Assassin

### In-memory хранилища с TTL

Данные хранятся в оперативной памяти с автоматической очисткой:

- `polls_storage` — опросы и результаты голосований (TTL: 24 часа)
- `photo_contest_storage` — фотографии участников конкурса (макс. 22 участника)
- `forwarded_messages_storage` — связь пересланных сообщений (TTL: 1 час)
- `location_storage` — геопозиция места проведения
- `tournament_storage` — турнир бир-понга

Хранилища поддерживают методы `cleanup_old()` для удаления устаревших записей.

### SQLite база данных (игра Assassin)

Игра Assassin использует персистентное хранилище в SQLite (`assassin_game.db`):

- `game` — информация об играх (статус, тестовый режим, победитель)
- `player` — игроки (реальные и виртуальные)
- `contract` — активные задания (убийца → цель + оружие + локация)
- `kill_log` — история убийств с временными метками
- `weapon` — список оружий
- `location` — список локаций

> **Примечание:** Игра Assassin полностью персистентна и переживает перезапуск бота.

### Middleware и безопасность

Бот защищен от злоупотреблений с помощью middleware:

- **Rate Limiting** — ограничение частоты запросов (10 сообщений/30 сек, 20 callback/30 сек)
- **Markdown Sanitization** — экранирование специальных символов в пользовательском вводе
- **Error Handling** — глобальный обработчик ошибок с логированием
- **Graceful Shutdown** — корректное закрытие соединений при остановке
- **Race Condition Prevention** — блокировки для критических операций (фото-конкурс)

### Фильтры

Переиспользуемые фильтры для обработчиков (`app/filters.py`):

- `IsAdminPrivateFilter` — приватные сообщения от администратора
- `IsUserPrivateFilter` — приватные сообщения от обычных пользователей
- `IsGroupChatFilter` — сообщения из группового чата
- Константы `IS_ADMIN_PRIVATE`, `IS_USER_PRIVATE`, `IS_GROUP_CHAT` для декораторов

## Масштабирование

Для production-использования рекомендуется:

1. **Персистентное хранилище** — заменить in-memory хранилища на Redis или PostgreSQL
2. **Webhook-режим** — использовать webhook вместо polling для снижения нагрузки
3. **Логирование** — настроить отправку логов в систему мониторинга (Sentry, ELK)
4. **Мониторинг** — добавить метрики (Prometheus) для отслеживания производительности
5. **Load Balancing** — при высокой нагрузке использовать несколько инстансов с общим хранилищем

### Уже реализовано

- ✅ **Graceful shutdown** — корректное закрытие соединений при остановке
- ✅ **Rate limiting** — защита от спама и DoS атак
- ✅ **Global error handler** — централизованная обработка исключений
- ✅ **TTL для хранилищ** — автоматическая очистка старых данных

## Лицензия

MIT
